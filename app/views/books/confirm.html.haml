.row
  .col-xs-12
    %h2 登録する本の確認
  .col-xs-3
    - book_image = @book_info.items.last.get('MediumImage/URL') || 'no_image_book.gif'
    = link_to @book_info.items.last.get('DetailPageURL'), target: '_blank' do
      = image_tag book_image, class: 'img-responsive'
  .col-xs-9
    = form_for @book do |f|
      .form-group
        = f.label :asin
        = @book_info.items.last.get('ASIN')
        = f.hidden_field :asin, value: @book_info.items.last.get('ASIN')
      .form-group
        = f.label :title
        = @book_info.items.last.get("ItemAttributes/Title")
        = f.hidden_field :title, value: @book_info.items.last.get("ItemAttributes/Title")
      = link_to '戻る', 'javascript: history.back()', class: 'btn btn-default'
      = f.submit '登録する', class: 'btn btn-default'
